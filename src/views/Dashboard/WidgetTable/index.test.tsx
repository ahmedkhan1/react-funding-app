import { shallow } from 'enzyme';
import WidgetTable from './index';
import 'setupTest';

describe('WidgetTable Component:', () => {
  it('renders without crashing', () => {
    // shallow(<WidgetTable />);
  });
});
